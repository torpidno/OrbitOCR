<UserControl x:Class="OrbitOCR.UI.ActionMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignWidth="500" d:DesignHeight="120">

    <UserControl.Resources>
        <!-- Standard Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2E3D"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#4B5563"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3B4254"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E2330"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Blue Accent Button (For Text Copy) -->
        <Style x:Key="PrimaryBlueButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#93C5FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E40AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Purple/Lens Accent Button (For Image/Lens Search) -->
        <Style x:Key="PrimaryPurpleButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="BorderBrush" Value="#A78BFA"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6D28D9"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#C4B5FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5B21B6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon / Mini Close Button -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main Floating Container with Pixel Glow Shadow -->
    <Border Background="#161822"
            BorderBrush="#384055"
            BorderThickness="1"
            CornerRadius="18"
            Padding="12,10"
            Margin="6">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="4" Opacity="0.7"/>
        </Border.Effect>

        <StackPanel Orientation="Vertical" Width="460">
            <!-- Header: Mode Badge & Preview -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="BadgeBorder" Background="#22304A" CornerRadius="10" Padding="8,3" Margin="0,0,8,0">
                        <TextBlock x:Name="StatusBadge" Text="📝 Text Selected" FontSize="11" FontWeight="Bold" Foreground="#60A5FA"/>
                    </Border>
                    <TextBlock x:Name="TextPreview" 
                               Text="Select any text or circle an image" 
                               FontSize="12" 
                               Foreground="#E2E8F0" 
                               TextTrimming="CharacterEllipsis" 
                               MaxWidth="320" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1" x:Name="BtnClose" Style="{StaticResource IconButton}" ToolTip="Close (Esc)" Width="24" Height="24">
                    <Path Data="M 2,2 L 10,10 M 10,2 L 2,10" Stroke="#9CA3AF" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                </Button>
            </Grid>

            <!-- Separator Line -->
            <Rectangle Height="1" Fill="#272D3D" Margin="0,0,0,8"/>

            <!-- Action Buttons Row -->
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <!-- Search Google Lens (Primary for Image Mode) -->
                <Button x:Name="BtnSearchLens" Style="{StaticResource PrimaryPurpleButtonStyle}" Margin="0,0,6,0" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 2,5 L 4,2 L 10,2 L 12,5 L 13,5 A 1,1 0 0 1 14,6 L 14,12 A 1,1 0 0 1 13,13 L 1,13 A 1,1 0 0 1 0,12 L 0,6 A 1,1 0 0 1 1,5 Z M 7,5.5 A 2.5,2.5 0 1 0 7,10.5 A 2.5,2.5 0 1 0 7,5.5"
                              Stroke="White" StrokeThickness="1.4" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Search with Lens"/>
                    </StackPanel>
                </Button>

                <!-- Copy Text (Primary for Text Mode) -->
                <Button x:Name="BtnCopyText" Style="{StaticResource PrimaryBlueButtonStyle}" Margin="0,0,6,0">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 3,5 A 2,2 0 0 1 5,3 L 11,3 A 2,2 0 0 1 13,5 L 13,11 A 2,2 0 0 1 11,13 L 5,13 A 2,2 0 0 1 3,11 Z M 5,1 L 11,1 A 2,2 0 0 1 13,3"
                              Stroke="White" StrokeThickness="1.4" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtBtnCopyText" Text="Copy Text"/>
                    </StackPanel>
                </Button>

                <!-- Search Google -->
                <Button x:Name="BtnSearchGoogle" Style="{StaticResource ActionButtonStyle}" Margin="0,0,6,0">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 6.5,2 A 4.5,4.5 0 1 0 11,6.5 M 10,10 L 14,14"
                              Stroke="#60A5FA" StrokeThickness="1.5" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Search Google"/>
                    </StackPanel>
                </Button>

                <!-- Copy Image Bitmap -->
                <Button x:Name="BtnCopyImage" Style="{StaticResource ActionButtonStyle}" Margin="0,0,6,0" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 2,2 L 12,2 L 12,12 L 2,12 Z M 4,8 L 6,6 L 8,9 L 10,5 L 12,8"
                              Stroke="#38BDF8" StrokeThickness="1.4" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Copy Image"/>
                    </StackPanel>
                </Button>

                <!-- Save Image -->
                <Button x:Name="BtnSaveImage" Style="{StaticResource ActionButtonStyle}" ToolTip="Save image to file" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 2,2 L 10,2 L 12,4 L 12,12 L 2,12 Z M 4,2 L 4,5 L 10,5 L 10,2 M 4,8 L 10,8 L 10,12 L 4,12"
                              Stroke="#34D399" StrokeThickness="1.4" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Save Image"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </StackPanel>
    </Border>
</UserControl>
