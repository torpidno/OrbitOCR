<Window x:Class="OrbitOCR.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OrbitOCR Settings"
        Height="540" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#13141C"
        Foreground="#F3F4F6"
        FontFamily="Segoe UI Variable, Segoe UI, sans-serif">

    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="SettingCard" TargetType="Border">
            <Setter Property="Background" Value="#1C1F2B"/>
            <Setter Property="BorderBrush" Value="#2E3446"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- RadioButton Style -->
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252A3A"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#3E475E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#262C3D"/>
            <Setter Property="Foreground" Value="#D1D5DB"/>
            <Setter Property="BorderBrush" Value="#3E475E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#323A4F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#2563EB" Width="28" Height="28" CornerRadius="14" Margin="0,0,10,0" VerticalAlignment="Center">
                    <Path Data="M 7,14 A 7,7 0 1 1 21,14 A 7,7 0 1 1 7,14 M 14,10 L 14,18 M 10,14 L 18,14" 
                          Stroke="White" StrokeThickness="2" Width="14" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="OrbitOCR Settings" FontSize="20" FontWeight="Bold" Foreground="#F9FAFB"/>
            </StackPanel>
            <TextBlock Text="Configure triggers, OCR preferences, and behavior." FontSize="12" Foreground="#9CA3AF" Margin="38,2,0,0"/>
        </StackPanel>

        <!-- Main Settings Body -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,-6,0" Padding="0,0,6,0">
            <StackPanel Orientation="Vertical">
                <!-- Section 1: Global Trigger Hotkey -->
                <Border Style="{StaticResource SettingCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Global Snip Hotkey" FontWeight="SemiBold" FontSize="14" Foreground="#F3F4F6"/>
                            <Border HorizontalAlignment="Right" Background="#1E293B" BorderBrush="#3B82F6" BorderThickness="1" CornerRadius="6" Padding="8,2">
                                <TextBlock x:Name="TxtHotkeyPreview" Text="Ctrl + Shift + S" FontWeight="Bold" FontSize="12" Foreground="#60A5FA"/>
                            </Border>
                        </Grid>

                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <CheckBox x:Name="ChkCtrl" Content="Ctrl" Margin="0,0,16,6"/>
                            <CheckBox x:Name="ChkShift" Content="Shift" Margin="0,0,16,6"/>
                            <CheckBox x:Name="ChkAlt" Content="Alt" Margin="0,0,16,6"/>
                            <CheckBox x:Name="ChkWin" Content="Windows" Margin="0,0,16,6"/>
                        </WrapPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Key:" VerticalAlignment="Center" Foreground="#9CA3AF" Margin="0,0,12,0" FontSize="13"/>
                            <ComboBox Grid.Column="1" x:Name="CmbKey" Height="30" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Section 2: Selection Mode -->
                <Border Style="{StaticResource SettingCard}">
                    <StackPanel>
                        <TextBlock Text="Default Snip Interaction" FontWeight="SemiBold" FontSize="14" Foreground="#F3F4F6" Margin="0,0,0,10"/>
                        <RadioButton x:Name="RadioModeRect" Content="Rectangle Drag-Select (Standard snip)" Margin="0,0,0,8" IsChecked="True"/>
                        <RadioButton x:Name="RadioModeLasso" Content="Circle to Search (Pixel-style glowing lasso)"/>
                    </StackPanel>
                </Border>

                <!-- Section 3: OCR Language -->
                <Border Style="{StaticResource SettingCard}">
                    <StackPanel>
                        <TextBlock Text="Offline OCR Language" FontWeight="SemiBold" FontSize="14" Foreground="#F3F4F6" Margin="0,0,4,0"/>
                        <TextBlock Text="Powered by Windows.Media.Ocr native engine" FontSize="11" Foreground="#9CA3AF" Margin="0,0,0,10"/>
                        <ComboBox x:Name="CmbOcrLanguage" Height="30" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Section 4: Behaviors -->
                <Border Style="{StaticResource SettingCard}">
                    <StackPanel>
                        <TextBlock Text="Behavior &amp; Integration" FontWeight="SemiBold" FontSize="14" Foreground="#F3F4F6" Margin="0,0,0,10"/>
                        <CheckBox x:Name="ChkAutoCopy" Content="Auto-copy extracted text on snip completion" Margin="0,0,0,10"/>
                        <CheckBox x:Name="ChkPlaySounds" Content="Play subtle audio chime on text copy" Margin="0,0,0,10"/>
                        <CheckBox x:Name="ChkStartWithWindows" Content="Start OrbitOCR minimized with Windows"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" x:Name="BtnCancel" Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
            <Button Grid.Column="2" x:Name="BtnSave" Content="Save Changes" Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</Window>
